[package]
name = "site-builder"
version = "0.1.0"
edition = "2021"

[dependencies]
pulldown-cmark = { version = "0.12", features = ["html"] }
maud = "0.26"
katex = "0.4"
tiny_http = "0.12"
clap = { version = "4", features = ["derive"] }
